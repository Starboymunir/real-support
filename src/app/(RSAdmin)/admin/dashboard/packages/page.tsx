import PackagesListView from "./_components/view/packages-list-view";

export const metadata = {
  title: "Dashboard: Package List",
};

const PackageList = () => {
  return <PackagesListView />;
};

export default PackageList;
