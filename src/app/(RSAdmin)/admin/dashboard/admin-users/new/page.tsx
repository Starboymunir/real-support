// sections
import { UserCreateView } from "../_components/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "Dashboard: Create a new user",
};

export default function UserCreatePage() {
  return <UserCreateView />;
}
